numberformatexception at parsing:
- error with distance field
- error with calories field (comma)
The problem is related with the line.split method call

Average time and average pace is different
Average calculator method that returns average of all metrics given an arraylist of type run

add total running time

using dates, calculate daily average distance by day

create hashmap run distance - avg time to create a table with average time for all main distances

- Calculate distribution of paces across all runs